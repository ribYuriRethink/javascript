// const ds = require("./desafio");

deescribe("doubleANumber", () => {
  it("should return a double of the number", () => {
    const result = doubleANumber(2);
    expect(result).toBe(2);
  });
});
